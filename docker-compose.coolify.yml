version: '3'

services:
  backend:
    build: 
      context: ./backend
    ports:
      - "${BACKEND_PORT:-9000}:8000"
    volumes:
      - type: bind
        source: ${PWD}/Scores with Names.csv
        target: /app/Scores with Names.csv
    environment:
      - GOOGLE_SEARCH_API_KEY=${GOOGLE_SEARCH_API_KEY}
      - GOOGLE_SEARCH_CX=${GOOGLE_SEARCH_CX}
    restart: unless-stopped
    networks:
      - nds-network

  frontend:
    build:
      context: ./frontend
      args:
        - NODE_ENV=production
        - VITE_API_BASE_URL=${BACKEND_URL:-http://backend:8000}/api
    ports:
      - "${FRONTEND_PORT:-3000}:80"
    depends_on:
      - backend
    environment:
      - NODE_ENV=production
      - VITE_API_BASE_URL=${BACKEND_URL:-http://backend:8000}/api
    restart: unless-stopped
    networks:
      - nds-network

networks:
  nds-network:
    driver: bridge 